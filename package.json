{
  "name": "howlin-at-the-moon-fest",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npm --prefix functions run build && firebase deploy --only hosting,functions,firestore",
    "test": "vitest run",
    "validate:webe": "node --env-file=.env.local scripts/validate-webe-integration.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@tailwindcss/vite": "^4.1.16",
    "astro": "^6.0.5",
    "dialog-polyfill": "^0.5.6",
    "firebase-admin": "^13.6.0",
    "tailwindcss": "^4.1.16",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "firebase-tools": "^14.24.1",
    "vitest": "^1.6.0"
  }
}
